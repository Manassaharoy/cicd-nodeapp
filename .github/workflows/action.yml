name: Node.js CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "helloooooo"
          echo "helloooooo"
          npm i
          echo "helloooooo"
          npm start
